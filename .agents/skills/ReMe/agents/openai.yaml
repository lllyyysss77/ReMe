interface:
  display_name: "ReMe"
  short_description: "Repo-specific patterns and workflows for ReMe"
  default_prompt: "Use the ReMe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true